version: '3.8'

services:
  gcli2api:
    image: ghcr.io/cetaceang/gcli2api:latest
    container_name: gcli2api
    restart: unless-stopped
    network_mode: host
    environment:
      - PASSWORD=chan
      - PORT=7861
      # - GOOGLE_CREDENTIALS=${GOOGLE_CREDENTIALS}  # 可选：从环境变量传入凭证
    volumes:
      - ./data/creds:/app/creds
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import sys, urllib.request, os; port = os.environ.get('PORT', '7861'); req = urllib.request.Request(f'http://localhost:7861/v1/models', headers={'Authorization': 'Bearer ' + os.environ.get('PASSWORD', 'chan')}); sys.exit(0 
      interval: 30sif urllib.request.urlopen(req, timeout=5).getcode() == 200 else 1)\""]
      timeout: 10s
      retries: 3
      start_period: 40s
